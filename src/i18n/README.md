# `src/i18n` — Internationalization (Phase 2)

Locales: `en`, `fr`, `ar` (Arabic = RTL). Approach: `next-intl` with locale
routing/middleware. Message catalogs are ported from the prototype `i18n.js`.
